# 게이트 운영 실행 기록 (검증셋 2,024품목, 2026-09-16)

| 실행 | 결과 | 비고 |
| --- | --- | --- |
| run 35103043265 (부트스트랩) | MAE 2.06 / 2.17 / 1.62cm, 3축 ±3cm 62.5%, 러너 p50 89ms. 절대 상한 통과 | 8월 26일 로컬 평가와 소수 둘째 자리까지 동일. 별칭은 버전 4로 이동했으나 워밍 호출 권한 부족으로 잡 실패, 기준선 미등록 |
| run 35104097754 (기준선 비교) | 비열화 0, 통과. live → 버전 5, 기준선 승격 | CI 역할에 `lambda:InvokeFunction` 추가 후. 러너 p50 118ms(같은 모델, 러너 편차) |

`f2318d1/` 는 두 실행이 같은 이미지 태그를 써서 두 번째 결과로 덮였다. `baseline-summary.json` 은 승격 뒤 기준선.
